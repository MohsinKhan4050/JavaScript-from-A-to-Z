// Task 1:
// Create an array with these website pages:
// Now do this:
// 1. Find the total number of items in the array
// 2. Print the first item
// 3. Print the last item

let pages = ["Home", "About", "Services", "Contact"];

// total number of items in the array
console.log(pages.length);

//the first item;
console.log(pages[0]);

//the last item;
console.log(pages[pages.length - 1])