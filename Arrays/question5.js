// Task 3:
// Create an array of product categories:
// Now do this:
// 1. Remove the last item from the array
// 2. Print the updated array
// 3. Print the total number of remaining items

let catagories = ["Mobile", "Laptop", "Watch", "Camera"];

//remove last item
catagories.pop();

//updated array
console.log("The new array is :", catagories);

//total number of remaining items;

console.log("The remaining items is:", catagories.length)