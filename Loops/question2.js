/* REQUIREMENTS:
- Use a for loop
- Print all even numbers from 1 to 20
- Do NOT hardcode numbers manually
- Use console.log to display output

*/


for (let i = 1; i <= 20; i++) {
    if (i % 2 === 0)
        console.log(i);
}